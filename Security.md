AWS credentials only from wp-config.php

Never stored in DB

Least privilege IAM

Private bucket only

No public ACL

JSON never leaves server except S3

Queue contains metadata only

No referral content stored in logs

Failure emails contain no referral data